import React from "react";
import styles from "@/components/lexical/plugins/editorToolbarPlugin/fontSize/FontSize.module.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(styles);

const FontSize = () => {
  return <div></div>;
};

export default FontSize;
