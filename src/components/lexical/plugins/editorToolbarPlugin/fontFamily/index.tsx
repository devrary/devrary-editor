import React from "react";
import styles from "@/components/lexical/plugins/editorToolbarPlugin/fontFamily/FontFamily.module.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(styles);

const FontFamily = () => {
  return <div></div>;
};

export default FontFamily;
